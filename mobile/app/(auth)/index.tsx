import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const AuthScreen = () => {
    return (
        <SafeAreaView>
            <Text className="text-3xl text-black">Auth Screen</Text>
        </SafeAreaView>
    )
}

export default AuthScreen;